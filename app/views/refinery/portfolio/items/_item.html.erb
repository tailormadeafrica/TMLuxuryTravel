<% if item.image.present? %>
  <div class="col-xs-12 col-sm-4 col-md-3 col-lg-3 portfolio-item">
    <%= link_to(image_fu(item.image, "270x",:class => "img-responsive"), item.image.url, :title => item.title, :rel => 'group1',:class => "cboxElement") %>
    <div class="project-overlay"></div>
  </div>
<% end %>

